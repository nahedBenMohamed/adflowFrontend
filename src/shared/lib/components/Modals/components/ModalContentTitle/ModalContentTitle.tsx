import styled from 'styled-components';

export const ModalContentTitle = styled.div<{ $hasMarginTop?: boolean }>`
  font-weight: 600;
  font-size: 18px;
  line-height: 160%;
  text-align: center;

  color: var(--button-text-graphite-priory-text);
`;
