import styled from 'styled-components';
import { TruncateMixin } from '../../../../mixins';

export const MyDropdownTitleWrapper = styled.div`
  color: var(--button-text-graphite-priory-text);
  font-weight: 400;

  ${TruncateMixin}
`;
