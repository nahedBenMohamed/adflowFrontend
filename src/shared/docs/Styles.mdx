# Styles and css related documentation

<br />

## System classes

<br />

### Components

This classes are related to **specific components** and can be used to modify their behavior:

- `.workspace__DeleteButton--Root` – `DeleteButton.tsx`;
- `.workspace__DownloadButton--Root` – `DownloadButton.tsx`;
- `.workspace__PencilButton--Root` – `PencilButton.tsx`;
- `.workspace__MyDropdown--StyledDropdown` – `MyDropdown.tsx`;
- `.workspace__MyPopover--StyledDropdown` – `MyPopover.tsx`;
- `.workspace__OverlayingModal--Overlay` – `OverlayingModal.tsx` -> Overlay;
- `.workspace__OverlayingModal--Content` – `OverlayingModal.tsx` -> Content;
- `.workspace__ToastContainer` – `ToastContainer.tsx`;
- `.workspace__MultichatControlHeader--Root` – `MultichatControlHeader.tsx`;
- `.workspace__TelephonyModalHeader--Root` – `TelephonyModalHeader.tsx`;
- `.workspace__PrimaryButton--Root` – `PrimaryButton.tsx`;
- `.workspace__ActionMenu--Cancel` – `ActionMenu.tsx` -> Cancel button;
- `.workspace__FeedControlTab--Root` – `FeedControlTab.tsx`;
- `.workspace__ClearParticipantIcon--path` – `clear_participant.svg` in `shared/assets`;
- `.workspace__TopNoteInfo--Root` – `TopNoteInfo.tsx`;
- `.workspace__ChatHeaderProviderTab--StyledTab` – `ChatHeaderProviderTab.tsx` -> StyledTab;
- `.workspace__AppointmentEventHoverCard--Root` – `AppointmentEventHoverCard.tsx`;
- `.workspace__PickerButton--PickerValue` – `PickerButton.tsx` -> `PickerValue`;
- `.workspace__MultichatControl--Root` – `MultichatControl.tsx`.
