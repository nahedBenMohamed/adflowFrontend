import styled from 'styled-components';
import { BuilderStepOutlinedSection } from '../../../../../../shared';

export const ContentWrapper = styled(BuilderStepOutlinedSection)`
  flex-direction: column;
`;
