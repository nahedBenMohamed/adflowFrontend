import { LoginForm } from '../../shared';
import { LoginTemplate } from '../../templates';

const LoginPage = () => {
  return (
    <LoginTemplate>
      <LoginForm />
    </LoginTemplate>
  );
};

export { LoginPage };
