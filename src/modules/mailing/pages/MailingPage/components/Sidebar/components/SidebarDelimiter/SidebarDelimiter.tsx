import styled from 'styled-components';

export const SidebarDelimiter = styled.div`
  width: calc(100% - 32px);
  height: 1px;

  flex-shrink: 0;

  background-color: var(--graphite-graphite-80);
  margin: 16px auto;
`;
