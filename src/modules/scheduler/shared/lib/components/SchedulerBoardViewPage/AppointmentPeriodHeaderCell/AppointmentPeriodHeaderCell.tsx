import styled from 'styled-components';

export const AppointmentPeriodHeaderCell = styled.div`
  font-size: 16px;
  font-weight: 700;
  line-height: 24px;
  color: var(--button-text-graphite-priory-text);

  padding: 8px;
`;
