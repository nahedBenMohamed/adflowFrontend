import styled from 'styled-components';

export const UserSettingsPageTitle = styled.label`
  display: flex;
  align-items: center;
  gap: 12px;

  font-weight: 600;
  font-size: 18px;
  line-height: 24px;
  color: var(--button-text-graphite-priory-text);

  padding-left: 16px;
  margin-bottom: 16px;

  &:hover {
    cursor: pointer;
  }
`;
