import styled from 'styled-components';

export const PermissionItemColumnTitle = styled.span`
  font-size: 12px;
  font-weight: 400;
  line-height: 16px;
  text-align: center;
  color: var(--button-text-graphite-priory-text);

  padding: 0 4px;
`;
